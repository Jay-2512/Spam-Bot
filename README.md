# Spam-Bot
This is a simple python bot created using python3 and tkinter

## How to Run?

1. Install the required packages used in this program find it inside the req_pkgs.txt or run the installpkgs.bat 
2. Then open your terminal and run the main.py file (```python main.py```)
